<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:id="@+id/logo"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_gravity="center"
            android:layout_marginStart="16dp"
            android:layout_marginTop="170dp"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="16dp"
            android:background="@color/teal_200"
            android:backgroundTint="?colorSecondary"
            android:gravity="center"
            android:text="@string/app_name"
            android:textColor="?colorOnSecondary" />

        <!--android:foreground="?android:attr/selectableItemBackground"-->
        <!--style="@android:style/Widget.Button"-->
        <TextView
            android:id="@+id/create_button"
            style="@style/ButtonStyle"
            android:background="@drawable/button_ripple"
            android:text="@string/create_new_graph"
            app:drawableLeftCompat="@drawable/ic_add_24"
            app:layout_constraintEnd_toEndOf="parent" />

        <TextView
            android:id="@+id/load_button"
            style="@style/ButtonStyle"
            android:background="@drawable/button_ripple"
            android:gravity="center"
            android:text="@string/load_from_file"
            app:drawableLeftCompat="@drawable/ic_download_24" />

        <TextView
            android:id="@+id/customize_button"
            style="@style/ButtonStyle"
            android:background="@drawable/button_ripple"
            android:gravity="center"
            android:text="@string/choose_custom_theme" />

        <TextView
            android:id="@+id/help_button"
            style="@style/ButtonStyle"
            android:background="@drawable/button_ripple"
            android:gravity="center"
            android:text="@string/help"
            app:drawableLeftCompat="@drawable/ic_help_24" />

    </androidx.appcompat.widget.LinearLayoutCompat>

    <TextView
        android:layout_margin="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/authors"
        android:text="@string/authors" />

    <FrameLayout
        android:id="@+id/fragment_container"
        android:name="com.example.pathfinder.CustomizationFragment"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>